const contact = () => {
    const contact_tab = document.createElement('div');
    contact_tab.setAttribute('id', 'contact_tab');
    contact_tab.textContent = 'Contact';

    return contact_tab;
};

export default contact;